const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..');let files=[],fail=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['.git','node_modules','drafts'].includes(e.name))continue;const f=path.join(d,e.name);if(e.isDirectory())walk(f);else if(e.name.endsWith('.html'))files.push(f)}}walk(root);for(const f of files){const s=fs.readFileSync(f,'utf8');const count=(s.match(/\/css\/brand-theme\.css/g)||[]).length;if(count!==1)fail.push(path.relative(root,f)+': brand-theme count '+count)}if(fail.length){console.error('Brand theme check failed:',fail.length);console.error(fail.slice(0,50).join('\n'));process.exit(1)}console.log('Brand theme check passed. Checked '+files.length+' HTML pages.');